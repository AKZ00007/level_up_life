// lib/screens/main_tabs/power_stats_screen.dart

import 'package:flutter/material.dart';

class PowerStatsScreen extends StatelessWidget {
  const PowerStatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Power Stats Screen Content', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}