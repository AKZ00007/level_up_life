// libscreens/main_tabs/glory_board_screen.dart
import 'package:flutter/material.dart';

class GloryBoardScreen extends StatelessWidget {
  const GloryBoardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Glory Board Screen Content', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}